'use strict';

/**
 *  tenants-recommendation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::tenants-recommendation.tenants-recommendation');
